# DecisionLens — core stack (pin major versions for reproducibility)
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.3

xgboost==2.0.3
scikit-learn==1.4.1.post1
# 0.44.x has no cp312 Windows wheel; pip falls back to source build and often fails without MSVC.
shap==0.45.1

pandas==2.2.1
numpy==1.26.4
openpyxl==3.1.2
# UCI credit-default file is legacy .xls (not xlsx); pandas needs xlrd for that format.
xlrd==1.2.0

streamlit==1.32.2
httpx==0.27.0
plotly==5.18.0

matplotlib==3.8.3
joblib==1.3.2

pytest==8.0.2
